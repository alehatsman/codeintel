# rules

`stdlib.dl` — the shipped rule library, the place a new structural question goes.
A new question is a rule here, not a function, a CLI verb or an MCP tool. The
budget is 24 named predicates, asserted in CI (`docs/plan.md` M4).

Empty until M4.
